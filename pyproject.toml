[project]
name = "automcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click>=8.2.1",
    "requests>=2.32.4",
]

[project.scripts]
automcp = "automcp.main:cli"
